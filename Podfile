# Podfile
platform :ios, '16.2'  # Specify the iOS platform version
use_frameworks!         # Enable dynamic frameworks

target 'ExpenseTrackerApp' do
  pod 'DGCharts'  # Use DGCharts instead of Charts
end  # Ensure this closes the target block
